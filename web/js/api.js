/**
 * 西域果铺 — API 配置
 * ================================================================
 * 后端地址，按实际部署环境修改
 */
const API_BASE = window.location.hostname
  ? window.location.origin + '/api'
  : 'http://localhost:8080/api';
